# Neon PostgreSQL pooled connection string. Keep SSL enabled.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DATABASE?sslmode=require&channel_binding=require&pool_timeout=20"

# Generate a long random value for signing authentication cookies.
SESSION_SECRET="replace-with-a-long-random-secret"
