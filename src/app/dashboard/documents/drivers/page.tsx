import Link from "next/link";
import { Card } from "@/components/ui/card";
import { authenticatedUser } from "@/lib/access-control";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function DriverDocumentsPage(){const user=await authenticatedUser();const docs=await db.driverDocument.findMany({where:{driver:{companyId:user.companyId!}},include:{driver:{select:{id:true,name:true,employeeId:true}}},orderBy:{createdAt:"desc"}});return <div className="mx-auto max-w-[1400px]"><p className="text-xs font-medium text-slate-500">Document & Compliance</p><h1 className="mt-1 text-2xl font-bold">Driver documents</h1><p className="mt-1 text-sm text-slate-500">Review documents linked to existing Driver Management records.</p><Card className="mt-6 overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50"><tr>{["Document","Driver","Employee ID","Number","Issue date","Expiry","File"].map(x=><th key={x} className="px-4 py-3 text-xs font-semibold text-slate-600">{x}</th>)}</tr></thead><tbody>{docs.map(doc=><tr key={doc.id} className="border-t text-xs"><td className="px-4 py-4 font-semibold">{doc.documentName??doc.fileName}</td><td className="px-4 py-4"><Link href={`/dashboard/drivers/${doc.driver.id}`} className="font-semibold text-blue-600">{doc.driver.name}</Link></td><td className="px-4 py-4">{doc.driver.employeeId??"—"}</td><td className="px-4 py-4">{doc.documentNumber??"—"}</td><td className="px-4 py-4">{doc.issueDate?.toLocaleDateString("en-GB")??"—"}</td><td className="px-4 py-4">{doc.expiryDate?.toLocaleDateString("en-GB")??"—"}</td><td className="px-4 py-4">{doc.fileName}</td></tr>)}</tbody></table>{!docs.length&&<p className="p-10 text-center text-sm text-slate-500">No driver documents found.</p>}</Card></div>}
