import Link from "next/link";
import { Card } from "@/components/ui/card";
import { authenticatedUser } from "@/lib/access-control";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function TyreHistoryPage() { const user = await authenticatedUser(); const history = await db.tyreHistory.findMany({ where: { tyre: { vehicle: { companyId: user.companyId! } } }, include: { tyre: { include: { vehicle: { select: { id: true, vehicleName: true, registrationNumber: true } } } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <div className="mx-auto max-w-[1200px]"><p className="text-xs font-medium text-slate-500">Tyre & Wheel Management</p><h1 className="mt-1 text-2xl font-bold">Tyre history</h1><p className="mt-1 text-sm text-slate-500">Permanent tyre lifecycle events and movement records.</p><Card className="mt-6 overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50"><tr>{["Date","Tyre","Vehicle","Event","Description","Details"].map(x=><th key={x} className="px-4 py-3 text-xs font-semibold text-slate-600">{x}</th>)}</tr></thead><tbody>{history.map(h=><tr key={h.id} className="border-t text-xs"><td className="px-4 py-4">{h.createdAt.toLocaleString("en-GB", { dateStyle: "medium", timeStyle: "short" })}</td><td className="px-4 py-4 font-semibold">{h.tyre.id.slice(-8).toUpperCase()}</td><td className="px-4 py-4"><Link href={`/vehicles/${h.tyre.vehicle.id}?tab=Tyres`} className="font-semibold text-blue-600">{h.tyre.vehicle.vehicleName}</Link><br/>{h.tyre.vehicle.registrationNumber}</td><td className="px-4 py-4">{h.action.replaceAll("_", " ")}</td><td className="px-4 py-4">{h.description}</td><td className="px-4 py-4">{h.metadata ? JSON.stringify(h.metadata) : "—"}</td></tr>)}</tbody></table>{!history.length&&<p className="p-10 text-center text-sm text-slate-500">No tyre history recorded yet.</p>}</Card></div>; }
