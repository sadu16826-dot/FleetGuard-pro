import Link from "next/link";
import { Card } from "@/components/ui/card";
import { authenticatedUser } from "@/lib/access-control";
import { db } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function AccidentReportsPage(){const user=await authenticatedUser();const accidents=await db.accident.findMany({where:{vehicle:{companyId:user.companyId!}},include:{vehicle:{select:{id:true,vehicleName:true,registrationNumber:true}},driver:{select:{id:true,name:true,employeeId:true}}},orderBy:{date:"desc"},take:100});const counts={total:accidents.length,reported:accidents.filter(a=>a.status==="REPORTED").length,investigating:accidents.filter(a=>a.status==="INVESTIGATING").length,resolved:accidents.filter(a=>a.status==="RESOLVED").length};return <div className="mx-auto max-w-[1400px]"><p className="text-xs font-medium text-slate-500">Accident Management</p><h1 className="mt-1 text-2xl font-bold">Accident reports</h1><p className="mt-1 text-sm text-slate-500">Track vehicle accidents using existing vehicle and driver records.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(counts).map(([label,value])=><Card key={label} className="p-5"><p className="text-xs capitalize text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold">{value}</p></Card>)}</div><Card className="mt-6 overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50"><tr>{["Accident","Vehicle","Driver","Date","Location","Description","Status","Action"].map(x=><th key={x} className="whitespace-nowrap px-4 py-3 text-xs font-semibold text-slate-600">{x}</th>)}</tr></thead><tbody>{accidents.map(accident=><tr key={accident.id} className="border-t text-xs"><td className="px-4 py-4 font-semibold">{accident.id.slice(-8).toUpperCase()}</td><td className="px-4 py-4"><Link href={`/vehicles/${accident.vehicle.id}`} className="font-semibold text-blue-600">{accident.vehicle.vehicleName}</Link><br/>{accident.vehicle.registrationNumber}</td><td className="px-4 py-4">{accident.driver.name}</td><td className="px-4 py-4">{accident.date.toLocaleString("en-GB",{dateStyle:"medium",timeStyle:"short"})}</td><td className="px-4 py-4">{accident.location}</td><td className="max-w-[280px] px-4 py-4">{accident.description}</td><td className="px-4 py-4">{accident.status.replaceAll("_"," ")}</td><td className="px-4 py-4"><Link href={`/vehicles/${accident.vehicle.id}?tab=Accidents`} className="font-semibold text-blue-600">View</Link></td></tr>)}</tbody></table>{!accidents.length&&<p className="p-10 text-center text-sm text-slate-500">No accident reports found.</p>}</Card></div>}
