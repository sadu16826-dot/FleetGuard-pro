import { authenticatedUser } from "@/lib/access-control"; import { db } from "@/lib/db"; import { Card } from "@/components/ui/card"; import { InspectionDetailsModal } from "@/components/inspections/inspection-details-modal";
export const dynamic = "force-dynamic";
export default async function InspectionHistoryPage(){const user=await authenticatedUser();const records=await db.inspection.findMany({where:{companyId:user.companyId!,vehicleId:{not:null}},include:{vehicle:true,inspector:{select:{name:true}},evidence:{select:{id:true,fileName:true,mimeType:true,fileSize:true}}},orderBy:{inspectionDate:"desc"},take:100});return <div className="mx-auto max-w-[1400px]"><p className="text-xs font-medium text-slate-500">Inspection Management</p><h1 className="mt-1 text-2xl font-bold">Vehicle history</h1><p className="mt-1 text-sm text-slate-500">Review completed vehicle inspections and reported issues.</p><Card className="mt-6 overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50"><tr>{["Inspection","Vehicle","Date / time","Inspector","Status","Remarks","Evidence"].map(x=><th key={x} className="px-4 py-3 text-xs font-semibold text-slate-600">{x}</th>)}</tr></thead><tbody>{records.map(record=>record.vehicle?<tr key={record.id} className="border-t text-xs"><td className="px-4 py-4 font-semibold">{record.id.slice(-8).toUpperCase()}</td><td className="px-4 py-4">{record.vehicle.vehicleName}<br/><span className="text-slate-500">{record.vehicle.registrationNumber}</span></td><td className="whitespace-nowrap px-4 py-4">{record.inspectionDate.toLocaleString("en-GB",{dateStyle:"medium",timeStyle:"short"})}</td><td className="px-4 py-4">{record.inspector?.name??"—"}</td><td className="px-4 py-4"><span className="rounded-full bg-blue-50 px-2 py-1 text-[10px] font-semibold">{record.status.replaceAll("_"," ")}</span></td><td className="px-4 py-4"><InspectionDetailsModal inspection={{id:record.id,vehicleName:record.vehicle.vehicleName,registrationNumber:record.vehicle.registrationNumber,inspectionDate:record.inspectionDate.toISOString(),inspector:record.inspector?.name??"—",status:record.status,notes:record.notes,photos:record.photos,evidence:record.evidence}}/></td><td className="px-4 py-4">{record.evidence.length||"—"}</td></tr>:null)}</tbody></table>{!records.length&&<p className="p-10 text-center text-sm text-slate-500">No inspection history yet.</p>}</Card></div>}
