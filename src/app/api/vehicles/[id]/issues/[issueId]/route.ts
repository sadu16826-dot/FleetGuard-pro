import{NextResponse}from"next/server";import{db}from"@/lib/db";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string;issueId:string}>}){try{const{id,issueId}=await params;const input=await request.json() as{status?:string;resolutionNotes?:string};const issue=await db.vehicleIssue.findFirst({where:{id:issueId,vehicleId:id}});if(!issue)return NextResponse.json({message:"Issue could not be found."},{status:404});const resolving=input.status==="RESOLVED"||input.status==="CLOSED";const updated=await db.$transaction(async tx=>{const value=await tx.vehicleIssue.update({where:{id:issueId},data:{status:input.status as never,resolutionNotes:input.resolutionNotes,resolvedDate:resolving?new Date():undefined,resolvedBy:resolving?"Sadu Admin":undefined}});if(resolving)await tx.vehicleActivity.create({data:{vehicleId:id,userId:"dev-admin",action:"ISSUE_RESOLVED",description:`${issue.issueType.replaceAll("_"," ")} issue resolved`,metadata:{issueId}}});return value});return NextResponse.json(updated)}catch{return NextResponse.json({message:"Unable to update issue."},{status:500})}}
