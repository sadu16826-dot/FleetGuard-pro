import{NextResponse}from"next/server";import{db}from"@/lib/db";
type IssueInput={issueType?:string;description?:string;priority?:string;reportedDate?:string;status?:string;currentKm?:number;photos?:string[];safetyCritical?:boolean};
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;return NextResponse.json(await db.vehicleIssue.findMany({where:{vehicleId:id},orderBy:{createdAt:"desc"}}))}catch{return NextResponse.json({message:"Unable to load issues."},{status:500})}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const input=await request.json() as IssueInput;const vehicle=await db.vehicle.findUnique({where:{id}});if(!vehicle)return NextResponse.json({message:"Vehicle could not be found."},{status:404});if(!input.issueType||!input.description?.trim()||!input.priority)return NextResponse.json({message:"Issue type, description and priority are required."},{status:400});const issueType=input.issueType;const critical=input.priority==="CRITICAL"&&Boolean(input.safetyCritical);const issue=await db.$transaction(async tx=>{const created=await tx.vehicleIssue.create({data:{vehicleId:id,issueType:issueType as never,description:input.description!.trim(),priority:input.priority as never,status:(input.status||"OPEN") as never,reportedDate:input.reportedDate?new Date(input.reportedDate):new Date(),reportedBy:"Sadu Admin",currentKm:vehicle.currentKm,photos:input.photos??[],safetyCritical:critical}});if(critical)await tx.vehicle.update({where:{id},data:{status:"NOT_ROADWORTHY"}});await tx.vehicleActivity.create({data:{vehicleId:id,userId:"dev-admin",action:"ISSUE_REPORTED",description:`${input.priority} ${issueType.replaceAll("_"," ")} issue reported`,metadata:{issueId:created.id,safetyCritical:critical}}});return created});return NextResponse.json(issue,{status:201})}catch(error){if(process.env.NODE_ENV!=="production")console.error("Report issue failed",error);return NextResponse.json({message:"Unable to report vehicle issue."},{status:500})}}
