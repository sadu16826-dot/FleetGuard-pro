import { DashboardShell } from "@/components/dashboard/shell";
export default function VehicleLayout({ children }: { children: React.ReactNode }) { return <DashboardShell>{children}</DashboardShell>; }
