import Link from "next/link";
import { ArrowUpRight, Wrench } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Table, TableCell, TableHead } from "@/components/ui/table";

const alerts = [{vehicle:"CAR-001",issue:"Oil service",due:"500 km",status:"Upcoming",tone:"amber"},{vehicle:"CAR-015",issue:"Insurance",due:"10 days",status:"Warning",tone:"red"},{vehicle:"VAN-008",issue:"Tyre inspection",due:"3 days",status:"Upcoming",tone:"amber"},{vehicle:"TRK-021",issue:"Permit renewal",due:"21 days",status:"Scheduled",tone:"blue"}];
export function AlertTable() { return <Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 className="text-sm font-semibold">Maintenance alerts</h2><p className="mt-1 text-xs text-slate-500">Items requiring attention</p></div><Link href="/dashboard/maintenance" className="flex items-center gap-1 text-xs font-semibold text-blue-600">View all <ArrowUpRight size={13}/></Link></div><div className="overflow-x-auto"><Table className="min-w-[600px]"><thead className="bg-slate-50"><tr><TableHead>Vehicle</TableHead><TableHead>Issue</TableHead><TableHead>Due date</TableHead><TableHead>Status</TableHead></tr></thead><tbody>{alerts.map(a=><tr key={a.vehicle} className="border-t border-slate-100"><TableCell><span className="flex items-center gap-2.5 font-semibold"><span className="grid size-7 place-items-center rounded-md bg-slate-100 text-slate-500"><Wrench size={13}/></span>{a.vehicle}</span></TableCell><TableCell className="text-slate-600">{a.issue}</TableCell><TableCell className="text-slate-600">{a.due}</TableCell><TableCell><span className={`rounded-full px-2 py-1 text-[11px] font-semibold ${a.tone==="red"?"bg-red-50 text-red-700":a.tone==="blue"?"bg-blue-50 text-blue-700":"bg-amber-50 text-amber-700"}`}>{a.status}</span></TableCell></tr>)}</tbody></Table></div></Card>; }
